# Domain used by the browser to reach the lab (matches LAB_DOMAIN in root .env)
NEXT_PUBLIC_LAB_DOMAIN=localhost

# Local dev only — FastAPI URL for next.config.ts rewrites (not used in production)
API_URL=http://localhost:8000
